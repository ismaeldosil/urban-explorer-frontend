/**
 * Zone flags for Ionic/Angular
 *
 * IMPORTANT: Do NOT disable customElements as Ionic components are Web Components
 * and need Zone.js to track their events properly.
 */

// Optionally disable certain event patches for performance
// (window as any).__zone_symbol__UNPATCHED_EVENTS = ['scroll', 'mousemove'];
